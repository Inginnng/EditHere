# HelpDesign · 0.8.7

面向 AI 开发反馈的桌面截图批注工具。截图松手后直接进入批注，在同一个窗口中标记修改意见、移动或缩放组件，最后复制包含原图、批注与变化的 JSON。

当前分支为 `codex/native`，使用 C++20 + Qt 6 Widgets。已保留的 `desktop` 分支为 Windows WPF 0.2（`cf9c364`），`web` 分支为 Web 0.3（`66548c5`）。

## 版本管理

日常修复和小幅优化只增加末位补丁号，例如 `0.8.0 → 0.8.1 → 0.8.2`。中间位只在集中完成较大功能阶段、明确发布时增加；不再为每轮开发递增。第一位保留给明确的大版本发布，已有版本号和历史包保持不变。

产品版本唯一来源为 `CMakeLists.txt` 的 `project(... VERSION ...)`。运行时版本和 Mac 应用信息自动使用该值；成功链接后生成 `build/version.txt`（Mac 为 `build-macos/version.txt`），两平台打包脚本据此命名并随包附带 `version.txt`。修改版本后必须重新构建，避免将旧程序标记为新版本。产品版本与 JSON 格式独立管理：`0.8.7` 的精简反馈由 `feedback-v0.7.schema.json` 定义，完整项目使用 `project-v3.schema.json`。

## 运行

Windows 便携包：`dist/HelpDesign-0.8.7-win-x64.zip`。先从托盘退出旧版，再完整解压并运行 `HelpDesign.exe`。请保留同目录 DLL 和插件文件夹；无需安装 Qt、Python、Node 或 .NET。

下文列出默认快捷键，可在设置中修改对应键盘操作。首次手动启动先显示使用引导；完成或跳过后不再自动显示。之后按 **Ctrl + Shift + 2** 或点击托盘图标截图。程序先关闭自身菜单并让系统托盘面板失去焦点，再采集画面。只有截图遮罩置顶，编辑窗口是普通应用窗口。

- 悬停智能选块；滚轮向上选择更大区域，向下选择更小区域。所有已识别、包含鼠标位置的框都可切换，无须互为父子。
- 单击选块或拖动画框，**松开鼠标立即进入批注**。Esc 取消截图；Ctrl+C 可在悬停选区时直接复制。
- 批注栏从一开始就固定预留。点编号或移动箭头，在右侧直接编辑，离开输入框自动保存；右上角加号可添加全局意见。框选松开立即在侧栏输入批注，并保留手动区域供组件调整。
- 调整工具可移动或缩放批注框；单击编号编辑文字。顶部眼睛按钮隐藏画面标记，保留右侧批注。Ctrl+Z 撤销，Ctrl+Y 重做，Delete 删除。
- 空白处滚轮或 Ctrl+滚轮缩放整张图片；中键切换适应窗口与原尺寸。拖动顶栏，或按住空格/Alt 拖动画面，可移动窗口。
- 支持打开、拖入或粘贴 PNG/JPEG/WebP/BMP，以及重新打开 `.helpdesign` 项目或反馈 JSON。
- 关闭当前截图会提示保存未存修改，随后释放截图和切块缓存；程序仍驻留托盘。

Mac 编辑快捷键使用 Command，重做为 Command + Shift + Z，截图为 Command + Shift + 2。Mac 尚未实机验证。

## 使用引导与开机自启

- 首次手动启动在真实编辑窗口展示六步引导，可随时跳过；没有截图时使用示例，有截图时保留原内容。右上角 **?** 和设置底部的 **使用引导** 均可从头开始。引导可前后翻页，Esc 只退出引导。
- 引导状态与设置偏好分开保存，恢复默认设置不会让引导反复出现。从设置重看引导会关闭设置窗口，不保存尚未应用的修改。
- 在 **设置 → 默认行为 → 开机时启动 HelpDesign** 勾选并保存后开启，取消勾选再保存即可关闭，默认关闭。登录系统后仅驻留托盘，不自动截图；“启动后立即截图”只用于普通手动启动。
- Windows 使用当前用户的启动项，无需管理员权限。便携版启用后请保留程序所在文件夹；移动程序后需重新设置。Mac 使用系统登录项，若系统要求批准会明确提示；Mac 仍未实机验收。

## 0.8.7 当前修复

按版本回退要求继续使用 0.8.7；移除本地 0.8.8、0.8.9 交付包，本次修复保持 0.8.7 版本号。

- 修复无批注时提示重复出现：空白提示只创建一次，快速刷新、取消草稿或删除最后一条批注时切换显示，不再反复销毁重建。
- 修复输入批注时 Esc 被窗口关闭快捷键抢占的问题，按 Esc 仅取消当前批注编辑。
- 顶栏增加最小化按钮，可恢复继续编辑当前截图。
- 移动轨迹按实际调整的区域记录：整体移动大块只显示一条，随后单独调整小块才增加它从原图位置到最终位置的轨迹；同一区域反复调整更新同一条。撤销、重做和项目保存保留这些关系。
- 未批注的移动箭头不显示编号，点击箭头可添加说明；已有批注沿用右侧编号，点击编号或箭头可继续编辑。普通画布、大爆炸及带批注图片保持一致。反馈 JSON 仍仅包含原图、批注和重建结果所需的变化；区域轨迹关系只保存在可编辑项目中。
- 补齐 HelpDesign 应用图标，覆盖 Windows 可执行文件、运行窗口与托盘，并准备 Mac 应用图标资源。

## 0.8.7 视觉一致性与大爆炸动效

- 大爆炸光浪改为微弯的虹彩波前，叠加柔光、错位反射和细亮边；从右上角短暂聚光后掠向左下，边缘辉光随同一波前移动，约 1.54 秒后消散。浅色、深色分别控制亮度。
- 动效不获取焦点、不拦截鼠标、不改变截图或切块；关闭模式或窗口即停止。柔光条带按窗口尺寸缓存，播放时只合成移动；细亮边保持矢量绘制，没有引入实时模糊或新运行依赖。
- 选中工具的图标与背景统一使用蓝色状态，侧栏标题统一字号和字重；移除组件区重复竖线，批注编辑保留卡片选中态并减轻内部边框。
- “查看 JSON”使用查看语义图标，复制 JSON 保留主按钮。

## 0.8.6 批注与工具栏优化

- 长批注没有内部滚动条。超过两行时可展开全文，收起显示两行并渐隐第二行；新建或编辑另一条时自动收起前一条。添加超过十条后，新批注仍会自动滚入视野。
- 组件调整区固定在侧栏底部，上方细线分隔；批注列表独立滚动。“区域”更名“显示分解框”，批注计数为“批注 X 条”，坐标不再显示【】装饰。
- 蓝色主按钮直接复制 JSON，小图标打开“查看 JSON”。核心工具与可配置操作之间增加分隔线。
- “重新截图”“适应图片”也可配置进入工具栏。未勾选或窄窗口放不下的操作均在更多菜单可用，常驻工具始终保留。
- 普通及大爆炸框选完成后直接进入侧栏批注，同时保存切块。移动箭头默认更细、更透明，悬停时提高对比度；保存的批注图采用同样的细箭头。

## 0.8.5 批注与项目

- 批注改为侧栏行内编辑，失焦保存；支持全局意见、移动箭头批注和一键隐藏画面标记。紧凑卡片把编号、坐标、编辑和删除放在同一行。
- 大爆炸的精调面板与批注同屏。底部框选统一建立手动区域，开启大爆炸之前也可使用；移除重复的“移动组件”按钮和长提示行。
- 工具栏默认展示保存项目、保存图片、复制 JSON、导出 JSON、复制带批注图片，可从设置定制。底部更多菜单也可直接进入工具栏设置。
- `.helpdesign` 保存原图、批注和完整切块缓存，重新打开可继续编辑；Windows 首次保存时登记当前用户的文件关联，已有其他默认程序时不覆盖。给 AI 的 JSON 仍只包含原图、批注和实际变化。

## 0.8.4 大爆炸光浪

大爆炸开启时，宽幅半透明虹彩光带从右上扫向左下，先蓄势、加速，再柔和消退。光效约 1.28 秒，不拦截鼠标；关闭模式或窗口时停止。切块缓存、批注和导出行为保持原有设计。

## 0.8.3 设置与更新

- 默认行为可设置启动时立即截图 / 仅驻留托盘、默认标注工具、图片初始适应窗口 / 100%、导出 JSON 是否默认包含原图。工具和缩放设置用于下一张图片，保存项目始终内嵌原图。
- 保留 17 项快捷键和系统 / 亮色 / 暗色外观；新增字段兼容原有设置，无需重新配置。“恢复默认”只修改草稿，保存才生效。
- 托盘右键“检查更新”，或设置的“关于与更新”中手动检查。可开启启动时检查，默认关闭；仅发现较新正式版时通知，不自动下载或安装。
- 使用 GitHub Releases，按数字比较版本（如 0.8.10 大于 0.8.9），忽略测试版和草稿。超时、网络失败、私有仓库无权限及未发布均明确提示，不能等同于最新版。
- 更新只请求版本信息，不上传图片、批注或设置。当前仓库私有：若电脑安装了 GitHub CLI 并已登录有权限账号，则使用其现有认证；HelpDesign 不读取、保存或打包访问令牌。没有该登录环境时，可在浏览器登录后查看私有发布页；公开仓库不需要 CLI。
- Windows 便携包增加系统 HTTPS 后端，无需安装 OpenSSL。Mac 沿用 Qt 部署工具提供的 TLS 后端。

## 0.8.2 识别升级

保留色块与内容边缘检测，增加原始分辨率的横竖线、闭合网格和合并单元格检测。浅灰细线、暗色表格、短断线和交替行底色不再只依赖旧版固定边缘阈值；不将线条检测图缩到 1100 像素。

表格候选包含单元格、行、列、整表，滚轮仍遍历包含指针的全部已识别范围。密集场景优先保留整表及行列候选；最多保留 384 个结构候选、合计 480 个图像候选，超限时对单元格采样。大爆炸优先使用这些结构区域，避免被大量文字碎块挤掉。

实现参考 [OpenCV 方向性线条提取](https://docs.opencv.org/4.x/dd/dd7/tutorial_morph_lines_detection.html) 的思路，在现有 C++/Qt 中实现局部对比、短缺口连接及长直线筛选；没有打包 OpenCV 库或模型。无边框、旋转表格、过于模糊的线条、图片内部语义物体仍可能漏检，需要手动画框。

新增检测回归覆盖浅色/暗色/粗线、4K 密集网格、文字与交替底色、合并单元格、短断线、独立表格、边框图片及非表格纹理。测试样例中的 4K 浅灰 18×18 网格从旧版零候选提升为 324 个单元格及行列/整表；这不是所有真实页面的准确率承诺。

## 0.8.1 改进

- 大图透明背景仅绘制可见区域，减少放大后的重绘开销。
- 横向滚动和零增量滚轮不再误缩小选区；大爆炸拖动失焦时取消未提交操作。
- 导出先保存 JSON 和原图，附加预览失败会显示具体原因，不阻断核心反馈保存。
- 复制 JSON 时对剪贴板短暂占用自动重试，失败会明确提示，不再误报成功。
- 程序、Mac 元数据和打包文件名共用 CMake 版本，日常更新使用末位。

## 设置

右键任务栏右下角的 HelpDesign 托盘图标，选择“设置”。窗口包含“快捷键”“外观”“默认行为”“工具栏”和“关于与更新”。点击“保存”应用设置；快捷键和外观立即生效，默认工具/缩放用于下一张图片，启动行为用于下次启动。“取消”不改变当前配置。

可配置 21 项快捷键：截图为全局快捷键，在其他应用中也能触发；其余在 HelpDesign 编辑窗口中使用。点击输入框后按下新的组合键，清空可停用对应快捷键。“恢复默认”会重置当前设置草稿，仍需保存才生效。重复分配、系统占用或保存失败会显示原因并保留原有配置。

以下为 Windows 默认键位；Mac 常用编辑组合使用 Command，重做为 Command + Shift + Z，尚待实机验证。

| 范围 | 操作 | 默认快捷键 |
| --- | --- | --- |
| 全局 | 截图 | Ctrl + Shift + 2 |
| 应用内 | 打开图片或项目 | Ctrl + O |
| 应用内 | 粘贴图片 | Ctrl + V |
| 应用内 | 保存项目 | Ctrl + S |
| 应用内 | 查看 JSON | Ctrl + E |
| 应用内 | 复制带批注图片 | Ctrl + C |
| 应用内 | 撤销 / 重做 | Ctrl + Z / Ctrl + Y |
| 应用内 | 适应窗口 | Ctrl + 0 |
| 应用内 | 删除所选批注 | Delete |
| 应用内 | 取消操作 / 关闭截图 | Esc |
| 应用内 | 智能选块 / 点标注 / 框选标注 / 调整批注 | B / P / R / V |
| 应用内 | 切换大爆炸 / 调整组件 | E / M |
| 应用内 | 复制 JSON / 保存图片 / 隐藏标记 / 全局批注 | 默认未绑定 |

外观支持**跟随系统、亮色、暗色**，默认为跟随系统。切换后，编辑器、批注栏、组件侧栏、菜单和设置窗口统一更新；原图与导出图片的原始像素不受主题影响。

设置保存在当前用户的 Qt `AppConfigLocation/settings.ini`，关闭程序后仍保留。Windows 默认位置为 `%LOCALAPPDATA%/HelpDesign/HelpDesign/settings.ini`；Mac 由系统配置目录决定。

## 大爆炸

点击顶部“大爆炸”按钮，按钮变色，并播放从右上向左下扫过的虹彩波浪。组件直接在**当前窗口**内调整。

首次进入会按识别框切块；再次点击按钮关闭模式时，画面保留调整结果，切块缓存继续保留。重新开启会使用同一套切块及调整数据，不会重新切分；关闭当前截图后才清除缓存。

大爆炸开启时，右侧始终保留批注；点标注工具可在结果上添加意见，编号和移动箭头可直接点击编辑。智能工具用于选块和调整，框选用于补充手动区域；选中组件后可从右侧添加批注。组件调整区固定在侧栏底部，批注列表独立滚动。附着在组件上的批注随组件移动、缩放，编辑结果和批注使用同一套画布坐标。

| 操作 | 行为 |
| --- | --- |
| 未选中时悬停并滚轮 | 遍历包含鼠标的全部候选范围 |
| 单击候选范围 | 选中组件或组件组 |
| 拖动已选区域内部 | 移动组件 |
| 拖动边缘中点 | 改变宽度或高度 |
| 拖动角点 | 等比例缩放，无需 Shift |
| 指向已选组件时滚轮 | 等比例放大或缩小组件；指向空白处缩放整图 |
| 右侧 X、Y、宽度、高度、缩放 | 精确设置像素位置、尺寸和比例 |
| 方向键 | 移动 1 px |
| 右键、Esc 或“取消选择” | 返回悬停选块 |
| 底部“框选”后画框 | 添加可操作区域，爆炸前后均可使用 |
| Ctrl+滚轮 | 缩放视图 |

选择大框时，内部组件一起移动、缩放，并保留相对布局。支持撤销和重做。移动后原位置留空，以透明棋盘格显示；不补背景、不恢复被遮挡的内容。调整限制在原图大小的画布内。

## 精简 JSON

复制 JSON 与导出 JSON 采用同一种精简结构。导出窗口默认勾选包含原图；需要单独发送图片时，可取消勾选以缩短 JSON。Ctrl+S 保存 `.helpdesign` 完整项目，始终内嵌原图及切块缓存，供以后继续编辑；不会把完整项目结构塞进给 AI 的反馈 JSON。

| 字段 | 含义 |
| --- | --- |
| `image` | 可选的原图 PNG data URL：`data:image/png;base64,...`，保存的是调整前的完整截图 |
| `annotationSpace` | 固定为 `"result"`，表示批注坐标对应当前调整后的画面 |
| `annotations` | 文字 `text`，可带 `point` / `rectangle` 定位；全局意见仅含 `text`，移动箭头意见带对应变化的零基序号 `change` |
| `changes` | 仅记录位置或尺寸发生变化的区域：`from` 为原图坐标，`to` 为最终坐标 |

没有变化的组件不导出；不输出完整切片清单、组件组引用、哈希或时间戳。原图 Base64 会增加文件体积，这是将图片放进单个 JSON 的数据开销，与组件数量无关。

坐标以画布左上角为原点，单位为图像像素，与窗口位置和视图缩放无关。框使用 `x1/y1/x2/y2` 两个端点；右下边界不包含自身，宽高为 `x2-x1`、`y2-y1`。批注坐标为整数，布局变化坐标可含小数。

以下示例省略可选的 `image` 字段，展示一个区域向右下方移动后的批注：

```json
{
  "annotationSpace": "result",
  "annotations": [
    {"point": {"x": 240, "y": 180}, "text": "这里改为蓝色"},
    {"rectangle": {"x1": 200, "y1": 140, "x2": 400, "y2": 240}, "text": "增加圆角"}
  ],
  "changes": [
    {
      "from": {"x1": 100, "y1": 100, "x2": 300, "y2": 200},
      "to": {"x1": 200, "y1": 140, "x2": 400, "y2": 240}
    }
  ]
}
```

还原结果时，先从原图提取所有 `from` 区域并将原位置留空，再按 `changes` 数组顺序绘制到 `to`；最后在结果上解释或绘制批注。未移动区域保持原样。0.8 继续使用 0.7 JSON 格式，由 `schema/feedback-v0.7.schema.json` 定义。

| 格式 | 原图 | 批注坐标 | 布局信息 |
| --- | --- | --- | --- |
| 0.5 及以前的 v1/v1.1/v2 项目 | `capture.pngBase64` 可内嵌，或引用外部 PNG | 原图 | v2 保存全部切片、分组及原始/目标坐标 |
| 0.6 精简反馈 | 外部同名 PNG | 原图 | 仅变化的 `from/to` |
| 0.7 / 0.8 当前反馈 | `image` 可内嵌，界面默认包含 | 调整后的画面 | 仅变化的 `from/to` |
| 3.0.0 `.helpdesign` 项目 | 始终内嵌原图 | 调整后的画面 | 完整切块缓存和当前编辑状态 |

应用仍可打开旧版项目及 0.6 精简反馈，并将旧批注转换到调整结果的位置。取消包含原图时，重新打开需要配套的同名原图 PNG。`.helpdesign` 项目保存完整切块及编辑状态（`schemaVersion: "3.0.0"`），重新打开直接恢复。关闭截图会释放内存缓存。重新打开精简反馈时，从原图和变化重建布局。

## 识别与平台边界

图片识别完全在本机运行，采用颜色连通区域、边缘聚合及横竖线网格分析，不需要网络、账号或模型下载。它能识别卡片、图片和较清晰的轮廓，但不是 OCR 或语义物体识别，不能保证每个候选都是用户理解的组件；可手动画框补充。

Windows 使用 UI Automation、Mac 使用 Accessibility 读取目标应用公开的元素边界。探测在独立短进程中运行，超时会停止。识别信息不包含代码仓库里的 DOM、组件名或源码位置；网页 DOM 标注仍是独立方向。

当前每次截取一块显示器内的区域，不跨屏拼接。混合缩放、多屏布局、托盘展开面板、受保护画面及不同浏览器仍需要更多设备验证。

| 平台 | 状态 |
| --- | --- |
| Windows x64 | 在 Windows 11 开发与测试；最低构建目标为 Windows 10 1809+，未逐一测试旧系统 |
| Windows 截图与 UI Automation | 已用本程序真实测试窗口校验像素、物理尺寸和按钮边界 |
| Windows 托盘面板与多屏 | 仍需更多真实环境验收 |
| Mac | GitHub macOS 构建与自动测试已通过，生成临时签名 DMG；**尚未实机验收** |
| Mac ARM / Intel | 已编译 arm64 + x86_64 通用程序，CI 在 Apple 芯片环境运行测试；Intel 尚未运行验收 |

Mac 目标为 macOS 14+，使用 ScreenCaptureKit。首次截图需屏幕录制授权；系统元素识别需辅助功能授权，托盘菜单提供入口。普通图片导入无需这些权限。目前没有可用的 Mac 测试设备。公开 Mac 发行还需产品所有者的 Apple 签名和公证。

## 构建与验证

依赖：Qt **6.8.3**（qtbase、qtimageformats 动态库）、CMake 3.24+、Ninja、C++20 编译器。Windows 使用 MinGW GCC 13.1.0；Mac CI 固定 macOS 15 + Xcode 16.4，以匹配 Qt 6.8.3；Xcode 26 SDK 已移除该版本 Qt 链接的 AGL framework。

Windows PowerShell 7，CMake 在 PATH 中：

```powershell
./scripts/build-windows.ps1 -QtRoot C:/Qt/6.8.3/mingw_64 -Compiler C:/Qt/Tools/mingw1310_64/bin/g++.exe -Ninja C:/Tools/ninja.exe
./scripts/package-windows.ps1 -QtRoot C:/Qt/6.8.3/mingw_64 -CompilerBin C:/Qt/Tools/mingw1310_64/bin
```

构建脚本运行检测、核心数据、布局、界面、行内文本、画布交互、设置、更新和 Windows 平台九组测试。导出样本及窗口渲染截图存于 `artifacts/native-ui/`。安装 `jsonschema==4.26.0` 后，可运行 `python scripts/validate-exports.py` 独立校验导出。打包只写新目录；重复打包请传入新的 `-OutputDirectory`。

Mac 构建（CI 已执行，尚未实机验收）：

```bash
export QT_ROOT="$HOME/Qt/6.8.3/macos"
bash scripts/build-macos.sh
```

脚本生成通用 `.app` 和本地测试用 `.dmg`，使用临时签名。私有仓库的 [macOS 构建记录](https://github.com/Inginnng/HelpDesign/actions/workflows/native-macos.yml) 保留测试结果和 DMG；屏幕录制授权、辅助功能授权及多屏截图仍需实机验收。

Qt 与 MinGW 的许可、版权声明位于 `packaging/licenses/`，随运行包交付；对应 Qt 源码存于 `dist/native-sources/`。公开分发时需同时提供相应源码归档，见[第三方说明](packaging/THIRD-PARTY-NOTICES.md)。
