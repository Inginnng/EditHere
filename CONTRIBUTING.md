# 参与 EditHere

欢迎提交问题、建议和补丁。项目为 C++20 + Qt 6 Widgets，构建与测试步骤见 [开发说明](docs/DEVELOPMENT.md)。

## 报告问题

请附上系统版本、程序版本、复现步骤，以及方便分享的截图或示例项目。涉及安装或更新的问题，附上安装器的输出或日志会大幅加快定位。

## 提交补丁

1. 小改动直接开 PR；较大改动建议先开 issue 说明方案，避免白做。
2. 说明改动动机、影响范围与验证方式（跑过哪些测试、在哪个平台验证）。
3. 保持与现有代码一致的风格，不混入无关的格式调整或重构。
4. 涉及反馈 JSON 或项目文件格式的改动，请同时更新 `schema/` 下的对应文件与文档。
5. Windows PowerShell 脚本请以**带 BOM 的 UTF-8**保存：PowerShell 5.1 会把无 BOM 的 UTF-8 按系统 ANSI 代码页解码，中文字符串会破坏脚本解析。

## 贡献的许可

提交即表示你同意自己的贡献按仓库当前许可（[MIT License](LICENSE)）一并发布，并保留原作者的版权声明。

项目目前**不需要**签署 [CLA](CLA.md)。若未来需要统一行使权利（例如变更许可或提供商业授权），会提前通知并只对新启用的贡献生效。

## 名称与品牌

EditHere 的名称与标识不在 MIT 授权范围内。分发改过的版本请改用你自己的名称，并说明它是派生版本，详见 [商标与品牌使用政策](TRADEMARK-POLICY.md)。
